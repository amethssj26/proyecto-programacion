<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Login pfSense</title>
  <style>
    body {
      margin: 0;
      font-family: Arial, sans-serif;
      background-color: #0f3c74;
      color: white;
    }

    .top-bar {
      background-color: white;
      color: black;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px 20px;
    }

    .top-bar img {
      height: 40px;
    }

    .login-container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 90vh;
      flex-direction: column;
    }

    .login-box {
      background-color: #0f3c74;
      padding: 40px;
      border-radius: 8px;
      text-align: center;
    }

    .login-box h2 {
      margin-bottom: 30px;
      font-weight: normal;
      letter-spacing: 2px;
    }

    .login-box input {
      display: block;
      margin: 10px auto;
      padding: 10px;
      width: 250px;
      border: none;
      border-bottom: 1px solid white;
      background-color: transparent;
      color: white;
      font-size: 16px;
    }

    .login-box input::placeholder {
      color: #ccc;
    }

    .login-box button {
      margin-top: 20px;
      padding: 10px 20px;
      background-color: #49cf73;
      border: none;
      color: white;
      font-size: 16px;
      cursor: pointer;
      border-radius: 4px;
    }

    .login-box button:hover {
      background-color: #38b562;
    }
  </style>
</head>
<body>

  <div class="top-bar">
    <img src="logo.png" alt="pfSense Logo">
    <span>Login to pfSense</span>
  </div>

  <div class="login-container">
    <div class="login-box">
      <h2>SIGN IN</h2>
      <form id="loginForm">
        <input type="text" id="username" placeholder="Username" required>
        <input type="password" id="password" placeholder="Password" required>
        <button type="submit">SIGN IN</button>
      </form>
    </div>
  </div>

  <script>
    document.getElementById("loginForm").addEventListener("submit", function(e) {
      e.preventDefault();
      const user = document.getElementById("username").value;
      const pass = document.getElementById("password").value;

      // Guardar credenciales (solo con fines educativos)
      console.log("Usuario:", user);
      console.log("Contraseña:", pass);

      // Simulación de acción posterior
      alert("Error al iniciar sesión, intente nuevamente.");
    });
  </script>

</body>
</html>
